# BinAgg 再現追試レポート

差分プライバシー下の線形回帰手法 **BinAgg** [[1]](#ref1) の公式実装 [[2]](#ref2) を固定環境で動かし、
論文の中核的な主張（妥当な信頼区間・回帰の保証で測る合成データの有用性）を再現追試した実証研究レポートである。
対象は tpdp2026 Issue #16。

> 📘 本ページは論文体のレポート本体である。用語の定義は「[用語と記法](notation.html)」、手法の詳細は
> 「[BinAgg 手法詳細](method-binagg.html)」、データ前処理とドメイン知識は「[前処理とドメイン特化メモ](data-notes.html)」、
> 環境構築・再現手順は「[エンジニアリングと再現手順](engineering-notes.html)」、よくある疑問は「[QA・メモ](faq.html)」に分けて掲載している。

実行環境: Python 3.12.12 / numpy 2.4.6 / scipy 1.17.1 / pandas 3.0.3。全乱数はシード固定。
本レポートの数値は `results/` の生成物に対応する（再現手順は[エンジニアリングと再現手順](engineering-notes.html)を参照）。

---

## 1. アブストラクト

差分プライバシー（個々のレコードの有無が出力からほとんど判別できないことを保証する枠組み [[1]](#ref1)）
の下での線形回帰について、論文 [[1]](#ref1) が提案する手法 BinAgg の公式実装 [[2]](#ref2) を固定環境で動かし、
中核的な主張を追試した。**結論として、主張はおおむね再現された。** すなわち、(i) この手法が与える信頼区間は
ほぼ名目どおりの被覆を達成し、ノイズを考慮しない素朴な区間が被覆を大きく失うのと対照的であること、
(ii) 合成データに素朴に回帰すると係数が偏る一方、この手法のバイアス補正がそれを回復すること、
(iii) 実データでもプライバシーの強さに応じた精度のトレードオフが期待どおり観測されること、を確認した（§5）。

なお、評価データ（UCI Air Quality）に潜む欠損値表現（センチネル `-200`）の汚染を前処理で除去したうえで
E3 を再評価しており、その過程で実データ計画が著しく悪条件であることを確認した。残課題の方向性としては、
(a) その悪条件性への対処（標準化・共線性整理・予測誤差での評価）、(b) 論文本文の図表との定量的な突き合わせ、
(c) 設定を広げた感度分析、が挙げられる（§7）。

---

## 2. イントロダクション

差分プライバシー (DP) の下で線形回帰を行うと、集約量に加えるノイズが二つの歪みを生む。
第一に、ノイズ入りの二乗和が真の二乗和より系統的に大きくなり、点推定が 0 方向へ偏る。
第二に、ノイズの分散を無視した標準誤差は過小評価となり、信頼区間が狭くなりすぎて被覆を失う [[1]](#ref1)。
論文 [[1]](#ref1) の BinAgg は、バイアス補正項とサンドイッチ分散推定でこの二つに同時に対処し、
さらに同一のプライバシー予算で合成データも生成できると主張する。合成データの良否を
「周辺分布の保存」ではなく「**回帰分析の保証（点推定の正しさと信頼区間の妥当性）**」で測る点が特徴である。

本レポートの貢献は次の三点である。

1. 公式実装 [[2]](#ref2) を固定環境で動かし、論文の主張（妥当な信頼区間）を**被覆率**という直接的な指標で定量検証した（E1）。
2. 合成データ上の回帰について、補正なしの偏りと BinAgg の補正による回復を対比し、有用性を回帰の保証で測った（E2）。
3. 実データ（UCI Air Quality）で挙動を再現し、評価データに潜む欠損値表現（センチネル `-200`）の前処理上の落とし穴を
   特定・是正したうえで、実データ計画の悪条件性が係数レベルの有用性評価を左右することを示した（E3, §6, §7）。

---

## 3. 関連技術・対象手法の概要

BinAgg（Binning-Aggregation）は μ-GDP（Gaussian Differential Privacy。$\mu$ が小さいほど強い保護 [[1]](#ref1)）
の下で、(1) PrivTree でデータ空間を private に分割（binning）、(2) 各 bin の件数・特徴量の和・目的変数の和に
Gaussian ノイズを加える、(3) ノイズ入り集約からバイアス補正済みの重み付き最小二乗で回帰する、という流れを取る [[1]](#ref1)[[2]](#ref2)。
点推定と信頼区間は次式で与えられる（記号は[用語と記法](notation.html)、導出と直感は[BinAgg 手法詳細](method-binagg.html)を参照）。

$$\tilde\beta = \bigl(\tilde S^\top \tilde W \tilde S - \tilde D\bigr)^{-1}\tilde S^\top \tilde W \tilde t,
\qquad \tilde\Sigma = \tilde M^{-1}\,\tilde H\,\tilde M^{-1}.$$

合成データ生成（Algorithm 3）は同じ集約から bin 単位で標本を作り、回帰と同じノイズ実現を共有することで
**同一予算で回帰と合成の両方**を出力できる（Corollary 3.1）[[1]](#ref1)。実装は回帰のみ (A)・回帰＋合成 (B)・
合成のみ (C) の 3 つの使い方を提供する [[2]](#ref2)。

---

## 4. 実験設定と評価方法

評価は周辺分布の一致（TVD）ではなく**回帰の保証**を主軸に置く [[1]](#ref1)。指標・ベースライン・データの詳細な
定義は[用語と記法](notation.html)と[前処理とドメイン特化メモ](data-notes.html)に置き、ここでは要点のみ述べる。

- **主指標**: 95% CI 被覆率（妥当なら $\approx 0.95$）、係数バイアス、相対 L2 係数誤差
  $\lVert\hat\beta-\beta\rVert_2/\lVert\beta\rVert_2$、CI 幅。補助として合成と元のモーメント誤差。
- **ベースライン**: 非プライベート OLS（参照値）、naive 推定量（補正なし）、合成データへの素朴 OLS。
- **プライバシー予算**: μ-GDP で $\mu \in \{0.5,\,1.0,\,2.0,\,5.0\}$。
- **データ**: シミュレーション（$X\sim\mathrm{Uniform}(0,10)^3$、$\beta=[1.5,-2,0.5]$、$y=X\beta+\varepsilon,\ \varepsilon\sim\mathcal{N}(0,1)$、$n=2000$）と
  実データ（UCI Air Quality、目的 CO(GT)）。実データは欠損センチネル `-200` を除去し、約 9 割が欠損の
  NMHC(GT) を除外した後、$n=6941$・$d=11$ を用いる（前処理の詳細は[前処理とドメイン特化メモ](data-notes.html)）。
  定義域の上下限 (bounds) は DP の要請により **データではなく既知の生成分布・ドメイン知識から**与える [[1]](#ref1)。

### 4.1 評価の限界（先に明記）

本実験はシミュレーション 1 種類の生成過程と実データ 1 種類に基づく限定的な確認である。
一般化には設定（特徴分布・次元・条件数・誤差分布）と複数データへの拡張が必要であり、§7 に残課題として整理する。

---

## 5. 実験結果

### 5.1 E1 — 統計的保証: 95% CI 被覆率（シミュレーション, $T=300$ 試行/$\mu$）

![E1: CI coverage and coefficient bias vs μ](results/figures/e1_coverage.png)

| μ | 被覆率 BinAgg(補正) | 被覆率 naive | 平均\|バイアス\| BinAgg | 平均\|バイアス\| naive | 平均 CI 幅 |
|---:|---:|---:|---:|---:|---:|
| 0.5 | **0.917** | 0.077 | 0.064 | 0.066 | 0.518 |
| 1.0 | **0.931** | 0.149 | 0.032 | 0.032 | 0.291 |
| 2.0 | **0.934** | 0.227 | 0.019 | 0.019 | 0.181 |
| 5.0 | **0.918** | 0.318 | 0.012 | 0.012 | 0.103 |

- **事実**: BinAgg の補正版 CI は全 $\mu$ で被覆率 0.92–0.93 と名目水準 0.95 に近い。一方、ノイズの分散を無視した
  naive 区間の被覆率は 0.08–0.32 と大きく不足する。CI 幅は $\mu$ が増えるにつれ単調に縮小する（0.52→0.10）。
- **考察**: 「妥当な信頼区間」という主張は再現された。点推定のバイアスは本設定（条件数の良い一様計画）では
  補正版と naive でほぼ同等であり、両者を分けているのは点推定よりも**分散推定（サンドイッチ標準誤差）**だと
  解釈できる。被覆率が 0.95 を僅かに下回るのは、この信頼区間が漸近的であることに伴う有限標本の近似誤差と
  整合的である（§7。背景は[QA・メモ](faq.html)）。

### 5.2 E2 — 合成データの回帰有用性とバイアス補正の効果（シミュレーション, 20 シード平均）

![E2: regression utility vs μ](results/figures/e2_utility.png)

相対 L2 係数誤差 $\lVert\hat\beta-\beta\rVert_2/\lVert\beta\rVert_2$（小さいほど良い）:

| μ | OLS(元データ, 参照) | naive OLS(合成) | BinAgg DP 回帰 ($\tilde\beta$) |
|---:|---:|---:|---:|
| 0.5 | 0.003 | 0.575 | **0.093** |
| 1.0 | 0.003 | 0.327 | **0.058** |
| 2.0 | 0.003 | 0.172 | **0.031** |
| 5.0 | 0.003 | 0.064 | **0.018** |

- **事実**: 合成データへ素朴に OLS を当てると係数誤差が大きい（$\mu=0.5$ で 0.58）。BinAgg のバイアス補正推定は
  同一データから誤差 0.09 と約 1 桁小さくなり、$\mu$ が大きくなるにつれて OLS(元データ) の 0.003 に漸近する。
- **考察**: 「合成データをそのまま回帰すると偏り、補正によって真値を回復できる」という主眼を確認した。
  有用性を回帰の保証で測る評価軸は、周辺分布の一致を見るより厳しく、下流の統計推論に直結する実用的な基準である。

### 5.3 E3 — 実データ再現: UCI Air Quality（$n=6941$, $d=11$, 5 シード平均）

欠損センチネル `-200` を除去し、約 9 割が欠損の NMHC(GT) を除外した**クリーンなデータ**で実行する
（前処理の詳細・経緯は[前処理とドメイン特化メモ](data-notes.html)）。これにより非プライベート OLS 参照と
合成モーメント比較がいずれも妥当になり、DP–OLS の係数一致を有用性指標として報告できるようになった。
実装の Option A/B/C をいずれも実行した（A: 回帰のみ、B: 回帰＋合成で予算共有、C: 合成のみ）。
モーメント誤差はクリーンな元データ（mean=1.766 / std=1.554）を基準とする。

![E3: CI width / DP-OLS coefficient agreement / synthetic moment error vs μ](results/figures/e3_airquality.png)

| μ | CI 幅（中央値） | DP–OLS 係数 relL2 | \|mean(y) 誤差\| | \|std(y) 誤差\| |
|---:|---:|---:|---:|---:|
| 0.5 | 0.044 | 5.38 | 0.797 | 1.908 |
| 1.0 | 0.044 | 3.64 | 0.446 | 1.226 |
| 2.0 | 0.057 | 3.43 | 0.161 | 0.641 |
| 5.0 | 0.014 | 1.18 | 0.043 | 0.229 |

- **事実**: 合成データのモーメント誤差は $\mu$ が増えるにつれ**単調に縮小**する（mean(y) 誤差 0.80→0.04、
  std(y) 誤差 1.91→0.23）。DP–OLS 係数の相対 L2 誤差も $\mu$ とともに改善する（5.38→1.18）。Option A/B/C は
  同一予算で問題なく動作した。
- **事実**: 一方この実データ計画は**著しく悪条件**である（$\mathrm{cond}(X^\top X)\approx 4.3\times10^8$、特徴量の
  標準偏差が 0.4〜406 とスケールが大きく異なり、$\lVert\hat\beta_{\text{OLS}}\rVert\approx 0.15$ と微小）。CI 幅は
  係数ごとの中央値では小さい（$\approx 0.04$）が、一部の悪条件方向で標準誤差が極端に大きくなり、平均は外れ値に
  支配される（そのため中央値で要約した）。
- **考察**: プライバシー予算を増やすほど精度が上がるという傾向は実データでも確認できた。ただし、点推定・CI の
  係数レベルの忠実度は設計行列の悪条件性に強く左右され、条件数の良いシミュレーション（E1/E2）ほど安定しない。
  実データでの係数一致は「手法の限界」というより「データ計画の条件数」に支配される、と解釈できる。

---

## 6. 考察

3 実験を通じて、論文 [[1]](#ref1) の中核的な主張は本実験条件で再現された。E1 は「妥当な信頼区間」を被覆率で
直接確認し、補正版と naive を分けるのが分散推定の質であることを示した。E2 は合成データの有用性が回帰の保証で
測れること、すなわち素朴 OLS の偏りを補正が回復することを示した。E3 は実データでの安定動作と μ トレードオフを確認した。

E3 では、当初の障害であった欠損センチネル `-200` の汚染を前処理で除去し（NMHC(GT) 列の除外と
`-200` 行の削除）、OLS 参照と合成モーメント比較を妥当化したうえで再評価した。その結果、合成データの
有用性（モーメント保存）はプライバシー予算とともに単調に改善することが確認できた。一方で、実データの
設計行列が著しく悪条件であるため、係数レベルの忠実度（DP–OLS 係数一致・CI 幅）は条件数に強く左右され、
シミュレーションほど安定しない。実データ評価では、有用性指標の選択そのものがデータ計画の素性に依存する点に
注意が要る（経緯と前処理は[前処理とドメイン特化メモ](data-notes.html)）。

---

## 7. 残課題

研究上・運用上の限界を正直に列挙する。

1. **実データ計画の悪条件性**: E3 の Air Quality は $\mathrm{cond}(X^\top X)\approx 4.3\times10^8$ と著しく悪条件で、
   係数レベルの忠実度（DP–OLS 係数一致・CI 幅）が不安定になる。条件数を整えた前処理（標準化・共線性の高い
   センサ列の整理・次元削減）や、係数一致以外の有用性指標（予測誤差など）での評価が望ましい。
   なお、当初の障害であった欠損センチネル `-200` の汚染は、NMHC(GT) 列の除外と `-200` 行の削除で対処済みである
   （詳細は[前処理ページ](data-notes.html)）。
2. **単一の生成過程・単一の実データ**: E1/E2 は 1 種類の線形生成過程、E3 は 1 データセットのみ。設定と
   複数データへの拡張が必要。
3. **漸近信頼区間の有限標本近似**: 被覆率が 0.95 を僅かに下回る点について、$n$・bin 数を増やした感度分析は未実施。
4. **論文本文との数値対応は未照合**: 本追試は実装 [[2]](#ref2) の挙動の再現であり、論文 [[1]](#ref1) の図表との
   定量的な一致は確認していない。
5. **bounds の与え方**: bounds を private に推定する経路（予算の一部を割り当てる方法）は評価していない。

---

## 8. 結論

公式実装 BinAgg を固定環境で動かし、論文 [[1]](#ref1) の中核的な主張を再現できた。すなわち、
(i) バイアス補正とサンドイッチ分散推定による 95% 信頼区間はほぼ名目どおりの被覆を達成し、ノイズを無視する
素朴な推定は被覆を失う (E1)、(ii) 合成データ上の回帰は補正なしでは偏り、BinAgg の補正が真値を回復する (E2)、
(iii) 実データでもプライバシーの強さに応じた精度のトレードオフが観測される (E3)。
「合成データの有用性を回帰の統計的保証で測る」という論文の評価軸の有効性を、被覆率という直接的な指標で
確認した点が本追試の主な貢献である。残課題は §7 の各項（特に `-200` の前処理の是正、論文数値との照合、
設定を広げた感度分析）である。

---

## 9. 参考文献

<ol class="refs">
<li id="ref1">Lin, S., Slavković, A., &amp; Bhoomireddy, D. R. (2026). <em>Differentially Private Linear Regression and Synthetic Data Generation with Statistical Guarantees.</em> AISTATS 2026 (PMLR). arXiv:2510.16974. <a href="https://arxiv.org/abs/2510.16974">https://arxiv.org/abs/2510.16974</a></li>
<li id="ref2">BinAgg (Python package). Shuronglin/BinAgg, commit 13c09bb (2026-05-27). <a href="https://github.com/Shuronglin/BinAgg">https://github.com/Shuronglin/BinAgg</a></li>
<li id="ref3">UCI Machine Learning Repository — Air Quality Data Set（同梱 <code>data/AirQualityUCI.csv</code>）.</li>
</ol>
